<!DOCTYPE html>
<html lang="en_GB">
  <head>
    <% include!("metadata.stpl") %>
    <% include!("favicon.stpl") %>
    <link rel="stylesheet" type="text/css" href="<%= site.assets.stylesheet %>" media="screen" />
    <script type="application/javascript" src="<%= site.assets.script %>"></script>
  </head>
  <body id="full">
    <% include!("themeswitch.html") %>
    <% include!("logo.svg") %>
    <% include!("banner.svg") %>
    <% include!("topnav.stpl") %>
    <span class="spacer"></span>
    <header><% content.header %></header>
    <main><% content.main %></main>
    <footer><% content.footer %></footer>
  </body>
</html>
