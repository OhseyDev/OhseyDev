<!DOCTYPE html>
<html lang="en_GB">
  <head>
    <% include!("metadata.stpl") %>
    <% include!("favicon.stpl") %>
    <link rel="stylesheet" type="text/css" href="<%= site.branding().stylesheet() %>" media="screen" />
    <script type="application/javascript" src="<%= site.branding().script() %>"></script>
  </head>
  <body>
    <% include!("themeswitch.html") %>
    <% include!("logo.svg") %>
    <header><% content.header() %></header>
    <main><% content.main() %></main>
    <footer><% content.footer() %></footer>
  </body>
</html>
